services:
  - type: web
    name: openclaw
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /healthz
    envVars:
      - key: OPENCLAW_HEADLESS
        value: "true"
      - key: SETUP_PASSWORD
        sync: false
